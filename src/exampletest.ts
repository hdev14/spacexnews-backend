// import Mongo from './database/Mongo'

// describe('describe', () => {
//   beforeAll(async () => {
//     await Mongo.connect(process.env.MONGO_URL || '')
//   })

//   afterAll(async () => {
//     await Mongo.disconnect()
//   })

//   it('should ...', async () => {

//   })
// })
